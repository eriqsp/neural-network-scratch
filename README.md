# Neural Network - using only numpy

The idea is to build a neural network engine from the ground up using only numpy. Implements backpropagation and gradient descent from matrices multiplications.


